BRIDGE_URL=https://POD_ID-19124.proxy.runpod.net
BRIDGE_TOKEN=replace-with-worker-token
WORKER_ID=equipment-pc-01
POLL_SECONDS=5

# auto prefers plink.exe when it is beside this script or on PATH; otherwise it uses ssh.exe.
# Use plink when password auth is needed. Use ssh for key-based auth.
SSH_TOOL=auto

# Optional when plink.exe is not beside the script or on PATH.
# PLINK_PATH=C:\path\to\plink.exe
